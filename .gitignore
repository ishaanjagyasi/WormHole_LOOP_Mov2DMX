# Public template repo (lives in its own git repo / GitHub project)
video-to-artnet-dmx/

# Virtual environments (regenerate from requirements; this one is Windows-only)
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]

# Local tooling / OS
#   .claude/settings.local.json IS tracked (permissions); chat transcripts live
#   in claude-session/ so the session can be resumed on another machine.
.DS_Store
Thumbs.db
